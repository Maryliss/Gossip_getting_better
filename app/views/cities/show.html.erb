
<h1>Voilà la liste des potins dans la ville de <%= @city.name %> </h1>

  <% @city.users.each do |user| %>
  <% user.potins.each do |potin| %>

<!-- Bootstrap table -->
        <div class="list-group">
          <a href="#" class="list-group-item list-group-item-action active">
          <div class="d-flex w-100 justify-content-between">
          <h5 class="mb-1"><%= potin.title %></h5>
          </div>
          <p class="mb-1"><%= potin.content %></p>
          <small><%= user.first_name%></small>
          </a>
        </div>
        <br>
        <!-- Fin Bootstrap table -->
        <% end %>
        <% end %>

